<Window x:Class="StaticCodeAnalyzer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:StaticCodeAnalyzer.Models"
        Title="Статический анализатор кода"
        Icon="Resources/icon.ico"
        Height="600" Width="1100"
        MinWidth="1000" MinHeight="500"
        WindowStartupLocation="CenterScreen"
        Background="#2D2D2D">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBackground" Color="#2D2D2D"/>
        <SolidColorBrush x:Key="SecondaryBackground" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="AccentColor" Color="#D32F2F"/>
        <SolidColorBrush x:Key="TextColor" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="ErrorColor" Color="#F44336"/>
        <SolidColorBrush x:Key="WarningColor" Color="#FF9800"/>
        <SolidColorBrush x:Key="InfoColor" Color="#2196F3"/>

        <!-- Стиль для заголовков столбцов DataGrid -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#3D3D3D"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#D32F2F"/>
            </Style.Resources>
        </Style>

        <!-- Стиль для ячеек DataGrid -->
        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="BorderBrush" Value="#3D3D3D"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="5,3"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentColor}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
        </Style>

        <!-- Стиль для TreeViewItem -->
        <Style TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="2,2"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Foreground" Value="#333333"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3D3D3D"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=TreeViewItem}}"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>

        <!-- Стиль для красной кнопки -->
        <Style x:Key="AnalyzerButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#B71C1C"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#C62828"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для серой кнопки -->
        <Style x:Key="GrayButton" TargetType="Button">
            <Setter Property="Background" Value="#555"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#333"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#444"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для ToggleButton (серая кнопка-переключатель) -->
        <Style x:Key="GrayToggleButton" TargetType="ToggleButton">
            <Setter Property="Background" Value="#555"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#333"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#444"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#2D2D2D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#B71C1C"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#B71C1C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Исправленный стиль для ComboBox (отображает выбранное значение и белый текст) -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Foreground="{TemplateBinding Foreground}"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="border"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="2">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0" 
                                                                  Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                                  HorizontalAlignment="Left" 
                                                                  VerticalAlignment="Center" 
                                                                  Margin="3,0,0,0"/>
                                                <Path Grid.Column="1" Data="M0,0 L4,4 L8,0" Fill="{StaticResource TextColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="BorderBrush" Value="#B71C1C"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder" Background="{StaticResource SecondaryBackground}" BorderBrush="{StaticResource AccentColor}" BorderThickness="1" CornerRadius="2">
                                    <ScrollViewer CanContentScroll="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="ToggleButton" Property="BorderBrush" Value="#B71C1C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель с кнопками -->
        <Border Grid.Row="0" Background="{StaticResource SecondaryBackground}" CornerRadius="5" Margin="0,0,0,10" Padding="10">
            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="Открыть файл" Width="100" Height="30" Margin="0,0,10,0"
                        Click="OpenFile_Click" Style="{StaticResource AnalyzerButton}"/>
                <Button Content="Открыть папку" Width="100" Height="30" Margin="0,0,10,0"
                        Click="OpenFolder_Click" Style="{StaticResource AnalyzerButton}"/>
                <Button Content="Анализировать" Width="100" Height="30" Margin="0,0,10,0"
                        Click="Analyze_Click" Style="{StaticResource AnalyzerButton}"/>
                <Button Content="Вставить код" Width="100" Height="30" Margin="0,0,10,0"
                        Click="PasteCode_Click" Style="{StaticResource AnalyzerButton}"/>
                <Button Content="Очистить" Width="100" Height="30" Margin="0,0,10,0"
                        Click="Clear_Click" Style="{StaticResource GrayButton}"/>
                <ToggleButton x:Name="ToggleTreePanel" Content="Скрыть дерево" Width="100" Height="30"
                              Checked="ToggleTreePanel_Checked" Unchecked="ToggleTreePanel_Unchecked"
                              Style="{StaticResource GrayToggleButton}"/>
            </WrapPanel>
        </Border>

        <!-- Панель фильтров -->
        <Border Grid.Row="1" Background="{StaticResource SecondaryBackground}" CornerRadius="5" Margin="0,0,0,10" Padding="5">
            <WrapPanel Orientation="Horizontal" Margin="5">
                <TextBlock Text="Фильтр:" Foreground="{StaticResource TextColor}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox x:Name="FilterSeverity" Width="100" Margin="0,0,10,0" SelectionChanged="Filter_Changed">
                    <ComboBoxItem Content="Все" IsSelected="True"/>
                    <ComboBoxItem Content="Критический"/>
                    <ComboBoxItem Content="Высокий"/>
                    <ComboBoxItem Content="Средний"/>
                    <ComboBoxItem Content="Низкий"/>
                </ComboBox>
                <TextBox x:Name="FilterCode" Width="100" Margin="0,0,10,0" TextChanged="Filter_Changed"
                         Foreground="{StaticResource TextColor}" Background="#1E1E1E" BorderBrush="#D32F2F"/>
                <TextBox x:Name="FilterFile" Width="200" Margin="0,0,10,0" TextChanged="Filter_Changed"
                         Foreground="{StaticResource TextColor}" Background="#1E1E1E" BorderBrush="#D32F2F"/>
                <Button Content="Сбросить" Width="80" Click="ResetFilter_Click" Style="{StaticResource GrayButton}"/>
            </WrapPanel>
        </Border>

        <!-- Основная область -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="TreeColumn" Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Левая панель: дерево файлов -->
            <Border Grid.Column="0" Background="{StaticResource SecondaryBackground}" CornerRadius="5" Margin="0,0,10,0" Padding="5">
                <ScrollViewer>
                    <TreeView x:Name="FilesTreeView" Background="Transparent" BorderThickness="0"
                              MouseDoubleClick="FilesTreeView_MouseDoubleClick">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate DataType="{x:Type local:FileTreeNode}" ItemsSource="{Binding Children}">
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox IsChecked="{Binding IsExcluded}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                </StackPanel>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </ScrollViewer>
            </Border>

            <!-- Правая панель: результаты анализа -->
            <Border Grid.Column="1" Background="{StaticResource SecondaryBackground}" CornerRadius="5" Padding="5">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Результаты анализа" FontWeight="Bold" FontSize="14"
                               Foreground="{StaticResource AccentColor}" Margin="0,0,0,5"/>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <DataGrid x:Name="ResultsGrid" AutoGenerateColumns="False" 
                                  Background="Transparent" Foreground="{StaticResource TextColor}"
                                  HeadersVisibility="Column" BorderThickness="0"
                                  CanUserResizeColumns="True" CanUserReorderColumns="True"
                                  CanUserAddRows="False"
                                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#3D3D3D"
                                  VerticalGridLinesBrush="Transparent"
                                  RowBackground="{StaticResource SecondaryBackground}"
                                  AlternatingRowBackground="#2A2A2A"
                                  Width="Auto"
                                  MouseDoubleClick="ResultsGrid_MouseDoubleClick">
                            <DataGrid.Resources>
                                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#D32F2F"/>
                            </DataGrid.Resources>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Важность" Binding="{Binding Severity}" 
                                                    Width="Auto" MinWidth="80" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Код" Binding="{Binding Code}" 
                                                    Width="Auto" MinWidth="70" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Файл" Binding="{Binding FilePath}" 
                                                    Width="Auto" MinWidth="150" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Класс" Binding="{Binding ContainingTypeName}" 
                                                    Width="Auto" MinWidth="100" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Метод" Binding="{Binding MethodName}" 
                                                    Width="Auto" MinWidth="100" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Строка" Binding="{Binding LineNumber}" 
                                                    Width="Auto" MinWidth="60" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Тип" Binding="{Binding Type}" 
                                                    Width="Auto" MinWidth="80" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" 
                                                    Width="Auto" MinWidth="200" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Предложение" Binding="{Binding Suggestion}" 
                                                    Width="Auto" MinWidth="200" IsReadOnly="True"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Grid>

        <!-- Строка статуса -->
        <StatusBar Grid.Row="3" Background="{StaticResource SecondaryBackground}" Margin="0,10,0,0" Padding="5">
            <TextBlock x:Name="StatusText" Foreground="{StaticResource TextColor}" Text="Готов"/>
        </StatusBar>
    </Grid>
</Window>